schema_version: 1
strategy: dockerfile
dockerfile: Dockerfile
run:
  port: 8083
healthcheck:
  path: /health
  port: 8083
env:
  - name: PORT
    default: "8083"
  - name: DATABASE_URL
    secret: true
